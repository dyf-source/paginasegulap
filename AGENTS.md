# Working Directories

- Root: `C:\TechnicalReport\Segulap`
- Source files are in the root directory (index.html, contact.html, etc.)
- Assets are in `assets/`
- Styles are in `assets/css/`

# Ignored Directories

- `./template/` — Ignore unless explicitly directed otherwise.

# Workflow

- Before making any major or structural change, first commit all unsaved and unsynced changes with `git add -A && git commit -m "checkpoint: ..."` to create a backup point, then proceed with the new changes.

## Guidelines
- Write clean, production-ready code.
- Always add comments explaining complex logic.
- Do not make major breaking changes without asking first.